<%= form_for(@article) do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title, placeholder: 'enter title', class: 'titre' %>
  <%= f.text_field :content, placeholder: 'body', class: 'body' %>
  <%= f.submit 'Update article!' %>
<% end %>
